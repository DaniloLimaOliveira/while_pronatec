{% extends '@SonataAdmin/standard_layout.html.twig' %}

{% block sonata_page_content %}

    <section class="content-header">
        <div class="sticky-wrapper"><nav class="navbar navbar-default" role="navigation">
                <div class="container-fluid">
                    <div class="navbar-collapse">
                        <ul class="nav navbar-nav navbar-left">
                            <h4>Relatório financeiro dos alunos<h4>
                        </ul>
                    </div>
                </div>
            </nav></div>
    </section>

    <section class="content">
        <div class="box box-primary">
            <div class="box-body">
                {% include '@SonataCore/FlashMessage/render.html.twig' %}
                <form class="sonata-filter-form form-horizontal" action="/admin/relatorioAluno/export" method="GET" role="form">
                    <div class="row">
                        <div class="col-sm-9">
                            <div class="form-group " id="filter-s5b661ace20bd0-aula.data" sonata-filter="true" style="">
                                <label for="filter_aula__data_value" class="col-sm-3 control-label">Data</label>
                                <div class="col-sm-4">
                                    <div id="filter_aula__data_value">
                                        <div>
                                            <label for="filter_aula__data_value_start">De</label>
                                            <input type="date" id="filter_aula__data_value_start" name="filter_aula__data_value_start" data-date-format="DD/MM/YYYY" class=" form-control" value="{{ (Model.dataIncio is defined) ? Model.dataIncio: '' }}">
                                        </div>
                                        <div>
                                            <label for="filter_aula__data_value_end">Até</label>
                                            <input type="date" id="filter_aula__data_value_end" name="filter_aula__data_value_end" data-date-format="DD/MM/YYYY" class=" form-control" value="{{ (Model.dataFim is defined) ? Model.dataFim: '' }}">
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="col-sm-3 text-center">
                            <div class="form-group">
                                <button type="submit" id="btnGerar" class="btn btn-primary" onblur="setTimeout(function(){$('#btnGerar').removeAttr('disabled');},1500);">
                                    <i class="fa fa-file-excel-o" aria-hidden="true"></i> Gerar relatório
                                </button>
                            </div>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </section>
{% endblock %}

